[project]
name = "stock-inventory-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain-core>=1.6.1",
    "langchain-ollama>=1.1.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
